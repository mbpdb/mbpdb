name: Simplified Workflow Test

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test-checkout:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
