name: Sync Notebooks to Personal Repo

on:
  push:
    branches: [ main ]
    paths: 
      - 'include/peptide/peptide/notebooks/**'

jobs:
  sync-notebooks:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout organization repo
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Need full history for subtree
        token: ${{ secrets.GITHUB_TOKEN }}
    
    - name: Configure git
      run: |
        git config --global user.name "kuhfeldrf"
        git config --global user.email "kuhfeldrf@gmail.com"
    
    - name: Debug token availability
      run: |
        if [ -z "${{ secrets.PERSONAL_GITHUB_TOKEN }}" ]; then
          echo "❌ PERSONAL_GITHUB_TOKEN is not set or empty"
          exit 1
        else
          echo "✅ PERSONAL_GITHUB_TOKEN is available"
        fi
    
    - name: Verify notebooks directory exists
      run: |
        if [ ! -d "include/peptide/peptide/notebooks" ]; then
          echo "Error: Notebooks directory not found"
          exit 1
        fi
        echo "Found notebooks directory with $(find include/peptide/peptide/notebooks -name "*.ipynb" | wc -l) notebook files"
    
    - name: Setup authenticated remote and push notebook contents
      run: |
        # Remove any existing peptiline remote
        git remote remove peptiline 2>/dev/null || true
        
        # Add remote with explicit token authentication
        git remote add peptiline https://${{ secrets.PERSONAL_GITHUB_TOKEN }}@github.com/Kuhfeldrf/peptiline.git
        
        # Verify remote is added correctly
        git remote -v
        
        # Clone the target repository to a temporary directory
        git clone https://${{ secrets.PERSONAL_GITHUB_TOKEN }}@github.com/Kuhfeldrf/peptiline.git temp_repo
        cd temp_repo
        
        # Switch to main branch (should already be on main, but being explicit)
        git checkout main
        
        # Copy notebook files to root of target repo (contents only, not the folder)
        cp -r ../include/peptide/peptide/notebooks/* . 2>/dev/null || echo "No files to copy"
        
        # Add and commit changes
        git add .
        if git diff --staged --quiet; then
          echo "No changes to commit"
        else
          git commit -m "Sync notebook contents from $(date)"
          echo "Pushing notebook contents to main branch"
          git push origin main
        fi
        
        # Return to original directory and cleanup
        cd ..
        rm -rf temp_repo
    
    - name: Verify sync success
      run: |
        echo "✅ Successfully synced notebook contents to main branch"
        echo "📂 Repository URL: https://github.com/Kuhfeldrf/peptiline"
        echo "📝 Note: Notebook files are now in the root of the repository (no notebooks folder)"
        echo "🔄 Changes are now live on the main branch"
